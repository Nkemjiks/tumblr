<div>
  <p><strong><%= comment.name %></strong></p>
  <p><%= comment.body %></p>
  <p><%= time_ago_in_words(comment.created_at) %></p>
  <%= link_to  "Delete", [comment.post, comment], method: :delete, data: { confirm: 'Are your sure?' } %>
</div>